name: flutter_barcode_scanner_example
description: Demonstrates how to use the flutter_scanner_plus plugin for Android and iOS.
publish_to: 'none'
version: 2.0.0-nullsafety.0

environment:
  sdk: '>=2.12.0-0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.1+1

dev_dependencies:
  flutter_lints: ^1.0.4
  flutter_test:
    sdk: flutter

  flutter_scanner_plus:
    path: ../


flutter:
  uses-material-design: true
